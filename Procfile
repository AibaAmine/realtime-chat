# Web service for HTTP and WebSockets (using Daphne)
web: daphne realtime_chat_backend.asgi:application --port $PORT --bind 0.0.0.0

# Optional: Worker service for long-running Channels tasks (if needed later)
# worker: python manage.py runworker
